namespace _17_задача
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // m – масса, p – плотность, V – объём
            // m = p * V
            // p = m / V
            int m = 100;
            int V = 30;
            Console.WriteLine("Плотность тела:");
            Console.WriteLine(m / V);
        }
    }
}
